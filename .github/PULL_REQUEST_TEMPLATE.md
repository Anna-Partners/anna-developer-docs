## Pages touched

<!-- List every article (section/slug) or reference-data file this PR changes. -->

## What changed and why

<!-- One paragraph. Link the forum thread / issue if this fixes a reported error. -->

## Verification

<!-- REQUIRED. How did you verify the corrected claim is true?
     e.g. "ran the snippet against runtime v1.1.0-beta.135 / CLI 0.1.49",
     "confirmed by engineering in forum thread #NNN", "matches @anna-ai/app-schema 0.19.0". -->

- Runtime version tested against:
- CLI version tested against (`anna-app --version`):

## Checklist

- [ ] `python scripts/validate.py` passes locally
- [ ] Code samples are runnable as written (no paraphrased snippets)
- [ ] No renamed slugs (renames need a `redirect_to` tombstone — see CONTRIBUTING)
- [ ] `verified_runtime` / `verified_cli` frontmatter updated on touched pages (if applicable)
